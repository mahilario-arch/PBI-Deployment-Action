name: CI-MainEntryRoute
on:
  workflow_call:
    inputs:
      path:
        required: false
        default: './Reports/${{ vars.TARGET_WORKSPACE_NAME }}'
        type: string
      filePattern:
        required: false
        default: './Reports/${{ vars.TARGET_WORKSPACE_NAME }}/*.pbix'
        type: string
      workspacename:
        required: false
        default: '${{ vars.TARGET_WORKSPACE_NAME }}'
        type: string
## commenting out below section as applicable for multi workspace publish##
    #  path1:
     #   required: false
      #  default: './Reports/BI (Pre Production - Test)'
       # type: string
      # filePattern1:
       # required: false
        # default: './Reports/BI (Pre Production - Test)/*.pbix'
        # type: string
      # workspacename1:
       # required: false
        # default: 'BI (Pre Production - Test)'
        # type: string
## commenting out above section as applicable for multi workspace publish##

    secrets:
      clientid:
        required: true
      secret:
        required: true
      tenantid:
        required: true

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Publish PBI contents for Workspace ${{ vars.TARGET_WORKSPACE_NAME }}
        shell: powershell
        run: |
          Get-ChildItem ${{ inputs.filePattern }}
          ./GitHub/run-task.ps1 -ClientId 21a1f2d2-3e39-4801-b5b9-ea20a6d44d64 -Secret SO38Q~evx2In.dquEV.ALZ6DdPfeFykO.u6~-bnZ -TenantId 441d4a8f-7999-4d6a-9670-672cf8d03e35 -FilePattern ${{ inputs.filePattern}} -WorkspaceName ${{ inputs.workspacename }} -Action 'Publish' -Create $false -Overwrite $true
# commenting out below section as applicable for multi workspace publish ###
     # - name: Call the PS script and publish PBI contents for LM
      #  shell: powershell
       # run: |
        #  Get-ChildItem ${{ inputs.filePattern1 }}
         # ./GitHub/run-task.ps1 -ClientId ${{ secrets.clientid }} -Secret ${{ secrets.secret }} -TenantId ${{ secrets.tenantid }} -FilePattern ${{ inputs.filePattern1}} -WorkspaceName ${{ inputs.workspacename1 }} -Action 'Publish' -Create $false -Overwrite $true
## commenting out above section as applicable for multi workspace publish##
